import 'package:flutter/material.dart';

class TarjetaCreditoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tarjetas de Crédito")),
      body: Center(child: Text("Contenido de Tarjetas de Crédito")),
    );
  }
}
